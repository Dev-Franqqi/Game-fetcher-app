/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.rawg.io"], // Add the hostname(s) of your image sources here
  },
};

module.exports = nextConfig
// next.config.js
